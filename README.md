# Cold Storage - Backend APIs

Basic Express + TypeScript MVC scaffold for backend APIs.

Run locally (development):

```bash
npm install
npm run dev
```

Build and run production:

```bash
npm run prod
```

API endpoints:

- GET / -> health
- GET /api/users -> sample users
